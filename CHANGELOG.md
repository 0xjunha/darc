# Changelog

All notable Darc release changes should be summarized here.

## Unreleased

- Drop shell metadata and unexpanded variable operands from file analytics.
- Preserve explicit tool paths and metadata-reference reads in file analytics.
- Preserve shell-embedded patch paths and escaped heredoc delimiters in file analytics.
- Drop process substitutions and preserve compound shell-test operands in file analytics.
- Install `darc` into `~/.local/bin` by default when using the shell installer.

## 0.1.0

- Initial release.
